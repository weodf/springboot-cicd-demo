1. Check the release:
   helm status {{ .Release.Name }} -n {{ .Release.Namespace }}

2. Check application resources:
   kubectl get deploy,pods,svc -n {{ .Release.Namespace }}

3. If ingress is disabled, use port-forward for a quick test:
   kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "springboot-cicd-demo.fullname" . }} 8080:8080
