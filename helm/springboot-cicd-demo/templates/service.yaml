apiVersion: v1
kind: Service
metadata:
  name: {{ include "springboot-cicd-demo.fullname" . }}
  labels:
    {{- include "springboot-cicd-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "springboot-cicd-demo.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
